import React from "react";

const StartEndSelect: React.FC = () => {
  return (
    <input
      type="text"
      style={{
        border: "1px solid #ccc",
        borderRadius: "4px",
        padding: "8px",
        fontSize: "16px",
        outline: "none",
        width: "200px", // Adjust width as needed
      }}
    />
  );
};

export default StartEndSelect;
